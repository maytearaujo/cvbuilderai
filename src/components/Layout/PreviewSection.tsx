import React from 'react';

const PreviewSection: React.FC = () => {
    return (
        <div className="preview-section">
            <h2>CV Preview</h2>
            {/* Add components to display the CV preview here */}
        </div>
    );
};

export default PreviewSection;